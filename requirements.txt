# Requires Python 3.11 (matches Dockerfile: python:3.11-slim).
# These pins do not resolve on Python 3.12+; use the container or a 3.11 venv.
Flask==3.0.0
gunicorn==21.2.0
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2
# Pinned explicitly: scikit-learn pulls these in, and RandomForest results can
# shift across their versions, which would break run-to-run reproducibility.
scipy==1.11.4
joblib==1.3.2
threadpoolctl==3.2.0
requests==2.31.0
pyyaml==6.0.1
